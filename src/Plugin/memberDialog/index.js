import VMemberDialog from './src/index.vue';

/* istanbul ignore next */
VMemberDialog.install = function (Vue) {
    Vue.component(VMemberDialog.name, VMemberDialog);
};

export default VMemberDialog;
